$('#my-posts').append("<%= j render(partial: 'popular_post', collection: @cards, as: :card) %>")
<% if @cards.next_page %>
  $('.pagination').replaceWith('<%= j will_paginate @cards %>');
<% else %>
  $(window).off('scroll');
  $('.pagination').remove();
<% end %>