<%=form_for msg, remote: true,authenticity_token: true do |f|%>
  <%= f.text_area :content, data: { textarea: "msg" }%>

  <%= f.hidden_field :chat_id, value: chat.id %>

  <%= f.submit "Send" %>
<%end%>