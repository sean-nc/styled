<%= form_for(current_user.likes.find_by(post_id: @post.id),
             html: { method: :delete },
             remote: true) do |f| %>
  <div><%= hidden_field_tag :post_id, @post.id %></div>
  <%= f.submit "Unlike" %>
<% end %>